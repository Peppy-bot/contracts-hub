// Per-arm governed joint setpoints, addressed by `arm_id`. Produced by
// openarm01_backbone (the bimanual coordination hub); consumed via `from_any`.
{
  peppy_schema: "interface/v1",
  manifest: {
    name: "openarm01_arm_governed_setpoint_source",
    tag: "v1",
  },
  interfaces: {
    topics: [
      {
        name: "arm_governed_setpoints",
        qos_profile: "sensor_data",
        message_format: {
          // Which arm (0 = left, 1 = right).
          arm_id: "u8",
          // Desired joint positions (rad), j1..j7.
          positions: {
            $type: "array",
            $items: "f64",
            $length: 7
          },
          // Desired joint velocities (rad/s), j1..j7 (feedforward).
          velocities: {
            $type: "array",
            $items: "f64",
            $length: 7
          }
        }
      }
    ]
  }
}
